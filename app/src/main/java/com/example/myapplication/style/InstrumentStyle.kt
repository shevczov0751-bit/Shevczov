package com.example.myapplication.style

import android.graphics.Color

object InstrumentStyle {

    /* Цвет корпуса прибора */
    val caseColor = Color.rgb(45, 45, 45)

    /* Основной фон шкалы */
    val dialColor = Color.rgb(15, 15, 15)

    /* Цвет делений */
    val scaleColor = Color.rgb(200, 200, 200)

    /* Цвет текста */
    val textColor = Color.WHITE

    /* Основная стрелка */
    val needleColor = Color.WHITE

    /* Вспомогательная стрелка */
    val secondaryNeedleColor = Color.rgb(255, 80, 80)

    /* Цвет неба (авиагоризонт) */
    val skyColor = Color.rgb(70, 140, 255)

    /* Цвет земли */
    val groundColor = Color.rgb(160, 90, 40)

    /* Цвет линии горизонта */
    val horizonLineColor = Color.WHITE

    /* Цвет рамки прибора */
    val bezelColor = Color.rgb(90, 90, 90)

    /* Внутренняя тень */
    val shadowColor = Color.argb(120, 0, 0, 0)

    /* Янтарная авиационная подсветка */
    val amberTextColor = Color.rgb(255, 200, 80)

}
